sudo: required
services:
  - docker

before_install:
  - docker build -t kman/react-test -f ./client/DOckerfile.dev ./client

script:
  - docker run kman/react-test npm test -- --coverage

after_success:
  - docker build -t kman/multi-client ./client
  - docker build -t kman/multi-nginx ./nginx
  - docker build -t kman/multi-server ./server
  - docker build -t kman/multi-worker ./worker
  #Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  #Push images to docker hub
  - docker push kman/multi-client
  - docker push kman/multi-nginx
  - docker push kman/multi-server
  - docker push kman/multi-worker
